import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IONIC_Demo_Components',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
